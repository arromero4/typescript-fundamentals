import * as MyCode from './myModule'
// import {PI as MyPI, Person, generaRandomNumber} from './myModule'

const user: MyCode.Person = {
    id: 1,
    name: 'andres'
}
console.log(MyCode.PI)
console.log(user)
console.log(MyCode.generaRandomNumber())